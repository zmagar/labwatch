services:
  labwatch:
    build: .
    image: labwatch:latest
    env_file: .env
    ports:
      - "8090:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    networks:
      - labwatch-net

networks:
  labwatch-net:
    external: true
